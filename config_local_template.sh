##__________________________________________________________________________________________________________
##### local configurations
##__________________________________________________________________________________________________________

readonly path=""                          #path to your working directory
readonly varscan=""                       #path to VarScan (.jar)
 
readonly input_dir=$path/input_dir        #do not change. the path to directory with initial data
readonly interm_dir=$path/interm_dir      #do not change. the path to directory with intermediate data
readonly output_dir=$path/output_dir      #do not change. the path to directory with results

readonly ref_dir=$input_dir/ref_dir       #do not change. the path to directory with the reference 
readonly samp_dir=$input_dir/samp_dir     #do not change. the path to directory with the sample data
readonly cont_dir=$input_dir/controls     #do not change. the path to directory with the controls
